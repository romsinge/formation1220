import { HiddenDisplayDirective } from './hidden-display.directive';

describe('HiddenDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
